- name: Render .rtorrent.rc
  template:
    src: "templates/home/rtorrent/.rtorrent.rc.j2"
    dest: "{{rtorrent['home']}}/.rtorrent.rc"
    owner: "{{rtorrent['user']}}"
    group: "{{rtorrent['group']}}"
    mode: '0644'
  become: yes
  notify:
    - restart rtorrent
